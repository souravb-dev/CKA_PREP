apiversion: v1
kind: Pod
metadata:
  name: test-pod
  label:
    app_type: frontend
spec:
  containers:
    - name: test-pod
      image: nginx
      command:
        - sh
        - -c
        - sleep 2000
